spring:
  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: guest
    password: guest
  config:
    import: optional:configserver:http://localhost:8888
  application:
    name: ideademo-rabbitmqproducer

server:
  port: 8070
